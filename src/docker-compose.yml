services:
  catalogdb:
    image: postgres:17

  catalog.api:
    image: eshop/catalog.api:latest
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.Api/Dockerfile

volumes:
  postgres_catalog:
